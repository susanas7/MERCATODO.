language: php

php:
  7.4.7

install:
  composer install

deploy:
  github-token: $GITHUB_TOKEN

before_script:
  - cp .env.travis .env
  - mysql -e 'CREATE DATABASE IF NOT EXISTS homestead_test; USE homestead_test;'
  - composer self-update
  - composer install --no-interaction

script:
  -vendor/bin/phpunit
